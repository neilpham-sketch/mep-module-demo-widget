# The "manage" env layer. Copy to apps/internal/.env
# It only selects which resource file loads: loadEnv('internal') reads apps/internal/.env
# first, then apps/internal/venv/.env.${NODE_ENV} (the actual config — see venv/.env.example).

NODE_ENV=dev
