# The "manage" env layer. Copy to apps/external/.env
# It only selects which resource file loads: loadEnv('external') reads apps/external/.env
# first, then apps/external/venv/.env.${NODE_ENV} (the actual config — see venv/.env.example).

NODE_ENV=dev
