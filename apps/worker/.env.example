# The "manage" env layer. Copy to apps/worker/.env
# It only selects which resource file loads: loadEnv('worker') reads apps/worker/.env
# first, then apps/worker/venv/.env.${NODE_ENV} (the actual config — see venv/.env.example).

NODE_ENV=dev
