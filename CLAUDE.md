# Demo Widget — Claude Code

Agent operating instructions for this repo live in [`AGENTS.md`](./AGENTS.md) — read that
file first. This file exists only so Claude Code (which looks for `CLAUDE.md`) finds the
same instructions Codex and other agents read from `AGENTS.md`, with nothing duplicated.
